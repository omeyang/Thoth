{
  "mcpServers": {
    "opentelemetry": {
      "type": "stdio",
      "command": "uvx",
      "args": ["opentelemetry-mcp"],
      "env": {
        "BACKEND_TYPE": "${OTEL_BACKEND_TYPE:-jaeger}",
        "BACKEND_URL": "${OTEL_BACKEND_URL:-http://localhost:16686}"
      }
    }
  }
}
